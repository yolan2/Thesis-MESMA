# Archived: enumerate_stage2_signal.R
# This script previously enumerated Stage-2 diagnostics. Stage-2 multi-stage processing
# has been removed from the main pipeline and the original script has been archived here.

# Original content archived for reference.

# --- Original file (archived) ---
# Run script and use helpers directly
# source('fit_veg_mixture_mesma.R')
#
# if (!exists('df_tasks') || is.null(df_tasks) || nrow(df_tasks) == 0) {
#   stop('df_tasks not found in workspace; cannot enumerate')
# }
#
# ... (rest of original script omitted) 
